# Image processing
Pillow>=10.0.0
imagehash>=4.3.1

# CLIP embeddings
sentence-transformers>=2.2.0
torch>=2.0.0

# Clustering
scikit-learn>=1.3.0
numpy>=1.24.0
hdbscan>=0.8.33  # Optional but recommended - better clustering than DBSCAN

# Web server
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6

# Database (sqlite3 is built-in)

# Progress bars
tqdm>=4.66.0
